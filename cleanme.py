# script to clean temporary files generated by installation

import shutil

paths = ['build', 'cbuild', 'dist', 'openql.egg-info', ]

for path in paths:
	try:
		shutil.rmtree(path)
	except OSError:
		pass
